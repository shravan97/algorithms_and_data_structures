#include <bits/stdc++.h>
using namespace std;
typedef long long int ll;

int main(int argc, char const *argv[])
{
	ll n;
	cin>>n;
	std::vector<ll> v;

	for (ll i = 0; i < n; ++i)
	{
		ll k;
		cin>>k;
		v.push_back(k);
	}
	return 0;
}
